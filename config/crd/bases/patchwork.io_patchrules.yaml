---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: patchrules.patchwork.io
spec:
  group: patchwork.io
  names:
    kind: PatchRule
    listKind: PatchRuleList
    plural: patchrules
    shortNames:
    - pr
    singular: patchrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.target.kind
      name: Target
      type: string
    - jsonPath: .spec.overwrite
      name: Overwrite
      type: boolean
    - jsonPath: .status.targetCount
      name: Targets
      type: integer
    - jsonPath: .status.active
      name: Active
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: PatchRule defines patches to apply to matching target resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PatchRuleSpec is the spec for a PatchRule resource.
            properties:
              additions:
                description: 'Additions: nested YAML of values to set, mirroring the
                  target structure.'
                type: object
                x-kubernetes-preserve-unknown-fields: true
              overwrite:
                default: false
                description: Overwrite existing values. When false (default), only
                  sets missing keys.
                type: boolean
              removals:
                description: 'Removals: nested YAML where leaves are arrays of key
                  names to remove.'
                type: object
                x-kubernetes-preserve-unknown-fields: true
              target:
                description: Target identifies which resources to patch.
                properties:
                  apiVersion:
                    description: APIVersion of the target (e.g. "networking.k8s.io/v1").
                    type: string
                  conditions:
                    description: 'Conditions: nested YAML of values that must all
                      match on the target.'
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                  kind:
                    description: Kind of the target (e.g. "Ingress").
                    type: string
                required:
                - apiVersion
                - kind
                type: object
            required:
            - target
            type: object
          status:
            description: PatchRuleStatus reports the observed state.
            properties:
              active:
                type: boolean
              observedGeneration:
                format: int64
                type: integer
              targetCount:
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
